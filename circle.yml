machine:
  environment:
    NODE_ENV: test
  node:
    version: 6.9.0

dependencies:
  cache_directories:
    - node_modules
  override:
    - npm install

test:
  pre:
    - npm run build:css
  #   - gulp build
  #   - gulp vendor
